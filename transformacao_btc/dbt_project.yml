name: 'transformacao_btc'
version: '1.0.0'
config-version: 2

profile: 'transformacao_btc'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  transformacao_btc:
    gold:
      +materialized: table
      +schema: gold

    silver:
      +materialized: view
      +schema: silver
    
    bronze:
      +materialized: view
      +schema: bronze
    